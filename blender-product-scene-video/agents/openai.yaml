interface:
  display_name: "Blender 产品场景视频"
  short_description: "先逐句细化动画脚本和真实界面证据，再画技术草图并搭建 Blender"
  default_prompt: "Use $blender-product-scene-video to inspect the project's UI参考图 directory, preserve every exact UI filename as the canonical state label, split my narration into clause-level visual beats, verify the real UI evidence for every beat, define emotion, motion, axes, lighting, and edit transitions, obtain script approval, then generate the annotated rough-sketch storyboard and build the approved Blender scene."
